#!/bin/bash

TOP_FIVE_FOLDERS=
MAX_LENGTH=0

function top_five_folders {
    COUNT=1
    while [ ${COUNT} -le ${2} ]; do
        TOP_FIVE_FOLDERS+="\t"${COUNT}
        TOP_FIVE_FOLDERS+=" - "
        TOP_FIVE_FOLDERS+=$(du -h ${1} | sort -rn | head -${COUNT} | tail +${COUNT} | awk '{print $2}')
        TOP_FIVE_FOLDERS+=", "
        TOP_FIVE_FOLDERS+=$(du -h ${1} | sort -rn | head -${COUNT} | tail +${COUNT} | awk '{print $1}')
        TOP_FIVE_FOLDERS+="\n"
        let COUNT++
    done 
}
